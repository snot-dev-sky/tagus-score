import React from 'react';
import './logo.css';

const Logo: React.FC = () => (
  <div className="logo">
    <span className="logo__mark" />
    <span className="logo__text">Tagus Score</span>
  </div>
);

export default Logo;
